<% provide(:title, 'All users') %>
<h1>All users</h1>
<h2> <%= link_to "Create User", new_user_path %> </h2>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>
